package upem.tasksAnd.start.Services;
/*
import upem.tasksAnd.start.models.Attachement;
import upem.tasksAnd.start.models.Audio;
import upem.tasksAnd.start.models.Geolocalisation;
import upem.tasksAnd.start.models.Video;

public class AttachementService {
    Attachement attachement;

    AttachementService() {
        attachement = new Attachement();
    }

    public void addAudio(int attachementid, Audio audio) {
        attachement.setAudioinside(true);
        attachement.setAudio(audio);
        //Store in json file
    }

    public void addVideo(int attachementid, Video video) {
        attachement.setVideoinside(true);
        attachement.setVideo(video);
        //Store in json file
    }

    public void addGeo(int attachementid, Geolocalisation geolocalisation) {
        attachement.setGeoinside(true);
        attachement.setGeolocalisation(geolocalisation);
        //Store in json file
    }
//To end later
}
*/